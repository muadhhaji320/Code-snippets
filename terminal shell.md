

```shell
$ ls -l  // This command lists the files in the current directory
$ /* This is a multi-line comment
     It provides more detailed information
     about the following command */
  ls -l
```
